---
description: Plan then implement a GitHub issue (chains /ralph_plan and /implement_plan)
argument-hint: "[issue number, e.g. #123]"
---

1. use SlashCommand() to call /ralph_plan with the given issue number
2. use SlashCommand() to call /implement_plan with the plan file /ralph_plan just created in `docs/plans/`
